{
  "name": "rektefe-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "cd rest-api && npm start",
    "build": "cd rest-api && npm run build",
    "install:all": "cd rest-api && npm ci"
  },
  "dependencies": {
    "expo-print": "~14.0.3",
    "gsap": "^3.13.0"
  }
}
