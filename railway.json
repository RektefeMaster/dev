{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo === Searching for index.js === && find /app -name index.js -type f && echo === Found dist directories === && find /app -type d -name dist && echo === Trying actual path === && node /app/rest-api/dist/rest-api/dist/index.js || echo === FAILED, trying root === && cd /app/rest-api && node dist/index.js'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

